meta {
  name: Delete Project
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/orgs/{{org_id}}/projects/{{project_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{org_api_key}}
}

docs {
  Delete a project and all its data (products, licenses, etc.).
}
