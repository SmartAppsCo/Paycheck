meta {
  name: Delete Project
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/orgs/{{org_id}}/projects/{{project_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{org_member_api_key}}
}

docs {
  Soft-delete a project (requires admin role).

  This performs a soft delete - the project can be restored later using
  POST /orgs/{org_id}/projects/{project_id}/restore.

  This will cascade soft-delete:
  - All products
  - All licenses

  Returns:
  { "success": true }
}
