meta {
  name: Remove Project Member
  type: http
  seq: 14
}

delete {
  url: {{base_url}}/orgs/{{org_id}}/projects/{{project_id}}/members/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{org_member_api_key}}
}

docs {
  Remove a member from a project.
}
