meta {
  name: Delete Member
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/orgs/{{org_id}}/members/{{member_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{org_api_key}}
}

docs {
  Delete a member (requires owner role).
  Cannot delete yourself.
}
