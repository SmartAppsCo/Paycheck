meta {
  name: Update Member
  type: http
  seq: 4
}

put {
  url: {{base_url}}/orgs/{{org_id}}/members/{{member_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{org_api_key}}
}

body:json {
  {
    "name": "Updated Name",
    "role": "admin"
  }
}

docs {
  Update a member (requires owner role).
  Cannot change your own role.
}
