meta {
  name: Get Operator
  type: http
  seq: 18
}

get {
  url: {{base_url}}/operators/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operator_api_key}}
}

docs {
  Get operator role for a user by their user_id.
  Returns 404 if the user is not an operator.
}
