meta {
  name: Delete Operator
  type: http
  seq: 20
}

delete {
  url: {{base_url}}/operators/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operator_api_key}}
}

docs {
  Remove operator role from a user (requires owner role).
  Cannot delete yourself.
}
